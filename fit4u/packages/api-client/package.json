{
  "name": "@fit4u/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Client API centralisé Fit4U — JWT, refresh token, retry, timeout, requestId, erreurs uniformes. Consommé par apps/mobile, apps/web et apps/admin.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "@fit4u/types": "workspace:*",
    "@tanstack/react-query": "^5.51.0"
  }
}
